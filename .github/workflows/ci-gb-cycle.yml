name: CI - gb-cycle

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/emulator-runner.yml
    with:
      emulator: gb-cycle
      needs_audio: false

  deploy-pages:
    needs: run
    uses: ./.github/workflows/deploy-pages.yml
